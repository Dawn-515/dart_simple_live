# simple_live_app

基于 `simple_live_core` 实现的跨平台 Flutter 客户端，支持移动端和桌面端。

## 简介

`simple_live_app` 是一个功能完善的聚合直播客户端，支持多个直播平台，提供统一的观看体验。

## 支持平台

### 直播平台

- 虎牙直播
- 斗鱼直播
- 哔哩哔哩直播
- 抖音直播

### 设备平台

- ✅ Android
- ✅ iOS
- 🟡 Windows (BETA)
- 🟡 MacOS (BETA)
- 🟡 Linux (BETA)

## 主要功能

### 直播功能

- ✅ 多平台直播间浏览
- ✅ 直播搜索（房间/主播）
- ✅ 分类浏览
- ✅ 推荐直播
- ✅ 多清晰度选择
- ✅ 实时弹幕显示
- ✅ 弹幕设置（透明度、大小、速度等）
- ✅ 全屏/窗口播放
- ✅ 屏幕旋转控制
- ✅ 亮度和音量控制
- ✅ 画中画 (PIP)

### 用户功能

- ✅ 关注主播
- ✅ 观看历史
- ✅ 收藏管理
- ✅ 多设备同步（WebDAV/SignalR）
- ✅ B站/抖音账号登录

### 界面功能

- ✅ 明暗主题切换
- ✅ 动态取色 (Material You)
- ✅ 国际化支持
- ✅ 自定义设置

## 环境要求

- Flutter SDK: `>=3.0.5 <4.0.0`
- Dart SDK: `>=3.0.5`

### 使用 FVM（推荐）

本项目使用 FVM 管理 Flutter 版本，项目中已配置 `.fvmrc` 文件。

```bash
# 安装 FVM
dart pub global activate fvm

# 在项目中使用
cd simple_live_app
fvm flutter pub get
```

详见主 README 文档中的 FVM 说明。

## 快速开始

### 1. 安装依赖

```bash
cd simple_live_app
fvm flutter pub get
```

### 2. 运行项目

```bash
# Android
fvm flutter run

# iOS
fvm flutter run

# Windows
fvm flutter run -d windows

# MacOS
fvm flutter run -d macos

# Linux
fvm flutter run -d linux
```

### 3. 构建发布版

```bash
# Android APK
fvm flutter build apk --release

# Android App Bundle
fvm flutter build appbundle --release

# iOS
fvm flutter build ios --release

# Windows
fvm flutter build windows --release

# MacOS
fvm flutter build macos --release

# Linux
fvm flutter build linux --release
```

## 项目结构

```
lib/
├── app/              # 应用程序核心
│   ├── controller/   # 全局控制器
│   ├── utils/        # 工具类
│   ├── app_style.dart
│   ├── constant.dart
│   ├── sites.dart    # 直播平台配置
│   └── utils.dart
├── models/          # 数据模型
│   ├── account/      # 账号模型
│   └── db/           # 数据库模型
├── modules/         # 功能模块
│   ├── category/     # 分类
│   ├── follow_user/  # 关注
│   ├── home/         # 首页
│   ├── live_room/    # 直播间
│   ├── mine/         # 我的
│   ├── search/       # 搜索
│   ├── settings/     # 设置
│   └── sync/         # 同步
├── requests/        # 网络请求
├── routes/          # 路由配置
├── services/        # 服务层
├── widgets/         # 公共组件
└── main.dart        # 入口文件
```

## 主要依赖

- **框架**: GetX (状态管理 + 路由)
- **网络**: Dio
- **数据库**: Hive
- **视频播放**: Media Kit
- **弹幕**: Canvas Danmaku
- **UI 组件**: Flutter Smart Dialog, Extended Image

## 配置说明

### 同步功能

APP 支持两种同步方式：

1. **WebDAV 同步**: 支持各类 WebDAV 服务（如 坚果云、ownCloud 等）
2. **SignalR 同步**: 支持 SignalR 服务器实时同步

### 账号登录

- **B站**: 支持扫码登录，登录后可获取关注列表
- **抖音**: 支持扫码登录，登录后可获取关注列表

## 开发说明

### 代码生成

项目使用 Hive 数据库，需要运行代码生成：

```bash
fvm flutter packages pub run build_runner build
```

### 调试日志

APP 内置了日志系统，可以在设置中查看和导出日志。

## TODO

- [ ] 优化桌面平台体验
- [ ] iOS 播放问题优化
- [ ] 全屏、非全屏弹幕样式分离
- [ ] 重写直播间播放页面
- [ ] 更多平台支持

## 注意事项

- Android 最低版本要求: API 21 (Android 5.0)
- iOS 最低版本要求: iOS 12.0
- 桌面端功能仍在完善中，可能存在一些问题

## 许可证

本项目仅用于学习交流，严禁用于商业目的。 